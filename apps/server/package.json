{
  "name": "@ai-notebook/server",
  "version": "0.0.1",
  "private": true,
  "module": "src/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "bun run --watch src/index.ts",
    "build": "bun build --compile --target bun --outfile dist/server src/index.ts",
    "lint": "eslint --fix ."
  },
  "dependencies": {
    "elysia": "latest",
    "surrealdb": "^1.3.2"
  },
  "devDependencies": {
    "bun-types": "latest"
  }
}
